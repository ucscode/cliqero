# Better Auth social providers. Copy to config/security/auth.yaml.
# Disabled providers are ignored; enabled providers require both credentials.
social:
  google:
    enabled: false
    client_id: ""
    client_secret: ""
