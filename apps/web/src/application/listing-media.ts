import {newId} from "@/kernel/ids";import type {Account} from "@/modules/identity/account";import type {ListingRepository} from "@/modules/listing/listing";import type {ListingMedia,ListingMediaRepository} from "@/modules/listing-media/media";import {generatedObjectKey,ObjectStorageRegistry} from "@/modules/listing-media/storage";import {inspectImage} from "@/modules/listing-media/image";import type {UnitOfWork} from "@/kernel/unit-of-work";
export class ListingMediaService {constructor(private listings:ListingRepository,private media:ListingMediaRepository,private storage:ObjectStorageRegistry,private uow:UnitOfWork){}async create(owner:Account,listingId:string,input:{bytes:Uint8Array;mimeType?:string;filename?:string;altText?:string;position?:number}){await this.owned(owner,listingId);const image=inspectImage(input.bytes,input.mimeType);const id=newId(),provider=this.storage.default(),key=generatedObjectKey(listingId,id,image.mimeType);const stored=await provider.put({key,bytes:input.bytes,mimeType:image.mimeType});const existing=await this.media.listByListing(listingId);const value:ListingMedia={id,listingId,storageProvider:stored.provider,storageContainer:stored.container,objectKey:stored.key,mimeType:image.mimeType,originalFilename:safeFilename(input.filename),byteSize:BigInt(input.bytes.byteLength),width:image.width,height:image.height,position:input.position??existing.length,altText:(input.altText??"").trim().slice(0,500),state:"active",createdAt:new Date()};try{await this.uow.transaction(()=>this.media.save(value));return value;}catch(error){await provider.delete(stored).catch(()=>undefined);throw error;}}async list(owner:Account,listingId:string){await this.owned(owner,listingId);return this.media.listByListing(listingId);}async get(owner:Account,listingId:string,id:string){await this.owned(owner,listingId);const value=await this.media.findById(id);if(!value||value.listingId!==listingId||value.state==="deleted")throw new Error("Listing media not found");return value;}async update(owner:Account,listingId:string,id:string,input:{altText?:string;position?:number}){const value=await this.get(owner,listingId,id);if(input.altText!==undefined)value.altText=input.altText.trim().slice(0,500);if(input.position!==undefined){if(!Number.isSafeInteger(input.position)||input.position<0)throw new Error("Media position is invalid");value.position=input.position;}await this.media.save(value);return value;}async requestDeletion(owner:Account,listingId:string,id:string){const value=await this.get(owner,listingId,id);value.state="deletion_pending";value.deletionRequestedAt=new Date();value.lastDeletionError=null;await this.media.save(value);return value;}publicUrl(value:ListingMedia){return this.storage.get(value.storageProvider).publicUrl({provider:value.storageProvider,container:value.storageContainer,key:value.objectKey});}private async owned(owner:Account,id:string){const listing=await this.listings.findById(id);if(!listing)throw new Error("Listing not found");if(listing.sellerId!==owner.id)throw new Error("Forbidden");return listing;}}
export class ListingMediaDeletionProcessor {constructor(private media:ListingMediaRepository,private storage:ObjectStorageRegistry){}findWork(limit=50){return this.media.findDeletionWork(limit);}async process(id:string){const value=await this.media.findById(id);if(!value||value.state!=="deletion_pending")return value;value.deletionAttemptedAt=new Date();try{await this.storage.get(value.storageProvider).delete({provider:value.storageProvider,container:value.storageContainer,key:value.objectKey});value.state="deleted";value.lastDeletionError=null;}catch(error){value.lastDeletionError=(error instanceof Error?error.message:String(error)).slice(0,1000);}await this.media.save(value);return value;}}
function safeFilename(value?:string){if(!value)return null;return value.replace(/[\u0000-\u001f\\/]/g,"_").slice(0,255);}
export function mediaView(value:ListingMedia,url:string){return {id:value.id,listing_id:value.listingId,url,mime_type:value.mimeType,original_filename:value.originalFilename,byte_size:value.byteSize.toString(),width:value.width,height:value.height,position:value.position,alt_text:value.altText,state:value.state,created_at:value.createdAt.toISOString()};}
