import {authenticatedAccount,apiError} from "../../../../../http";import {getContainer} from "@/infrastructure/container";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const a=await authenticatedAccount(request);if(!a)return Response.json({error:"Unauthorized"},{status:401});try{const c=getContainer();await c.operators.requireOperator(a.id);const key=request.headers.get("idempotency-key");if(!key)throw new Error("A valid Idempotency-Key is required");const original=await c.treasuryRepository.findById((await params).id);if(!original)throw new Error("Treasury entry not found");const e=await c.treasury.reverse(original,{actorId:a.id,idempotencyKey:key});return Response.json({id:e.id,direction:e.direction,amount_minor:e.amountMinor.toString(),source_id:e.sourceId},{status:201});}catch(e){return apiError(e);}}
