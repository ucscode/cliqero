import {authenticatedAccount,apiError} from "../../../http";import {getContainer} from "@/infrastructure/container";import type {TransferFormat} from "@/application/listing-transfer";
export async function POST(request:Request){const a=await authenticatedAccount(request);if(!a)return Response.json({error:"Unauthorized"},{status:401});try{const c=getContainer();await c.operators.requireCatalogueManager(a.id);const u=new URL(request.url),format=(u.searchParams.get("format")??"json") as TransferFormat,mode=(u.searchParams.get("mode")??"create") as "create"|"upsert";if(!["json","csv","yaml"].includes(format)||!["create","upsert"].includes(mode))return Response.json({error:"Invalid import format or mode"},{status:400});return Response.json(await c.listingTransfer.importCatalogue(a,{format,mode,body:await request.text()}),{status:207});}catch(e){return apiError(e);}}
