import {authenticatedAccount,apiError} from "../../../http";import {getContainer} from "@/infrastructure/container";import {serializeTransfer,type TransferFormat} from "@/application/listing-transfer";const mime={json:"application/json",csv:"text/csv; charset=utf-8",yaml:"application/yaml; charset=utf-8"};
export async function GET(request:Request){const a=await authenticatedAccount(request);if(!a)return Response.json({error:"Unauthorized"},{status:401});try{const c=getContainer();await c.operators.requireCatalogueManager(a.id);const f=(new URL(request.url).searchParams.get("format")??"json") as TransferFormat;if(!["json","csv","yaml"].includes(f))return Response.json({error:"Invalid export format"},{status:400});return new Response(serializeTransfer(await c.listingTransfer.exportCatalogue(a),f),{headers:{"content-type":mime[f]}});}catch(e){return apiError(e);}}
