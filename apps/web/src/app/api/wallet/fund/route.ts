import {z} from "zod";import {authenticatedAccount,apiError} from "../../http";import {getContainer} from "@/infrastructure/container";
const schema=z.object({amount_minor:z.string().regex(/^[1-9][0-9]*$/),provider:z.string().min(1),collection_currency:z.string().regex(/^[A-Z]{3}$/).optional()}).strict();
export async function POST(request:Request){const a=await authenticatedAccount(request);if(!a)return Response.json({error:"Unauthorized"},{status:401});const key=request.headers.get("idempotency-key");if(!key)return Response.json({error:"Idempotency-Key is required"},{status:400});try{const b=schema.parse(await request.json());const f=await getContainer().fundingService.create({accountId:a.id,amountMinor:BigInt(b.amount_minor),providerName:b.provider,idempotencyKey:key,collectionCurrency:b.collection_currency});return Response.json({id:f.id,state:f.state,amount_minor:f.canonicalAmount.minorAmount.toString(),currency:"USD",provider:f.providerName},{status:201});}catch(e){return apiError(e);}}
