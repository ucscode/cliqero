import {authenticatedAccount,apiError} from "../../http";import {getContainer} from "@/infrastructure/container";import {serializeTransfer,type TransferFormat} from "@/application/listing-transfer";
const mime={json:"application/json",csv:"text/csv; charset=utf-8",yaml:"application/yaml; charset=utf-8"};export async function GET(request:Request){const account=await authenticatedAccount(request);if(!account)return Response.json({error:"Unauthorized"},{status:401});try{const c=getContainer();await c.operators.requireCatalogueManager(account.id);const format=(new URL(request.url).searchParams.get("format")??"json") as TransferFormat;if(!["json","csv","yaml"].includes(format))return Response.json({error:"Invalid export format"},{status:400});const content=serializeTransfer(await c.listingTransfer.export(account),format);return new Response(content,{headers:{"content-type":mime[format],"content-disposition":`attachment; filename=cliqero-listings.${format}`}});}catch(error){return apiError(error);}}
