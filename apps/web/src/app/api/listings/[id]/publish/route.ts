import {authenticatedAccount,apiError} from "../../../http";import {getContainer} from "@/infrastructure/container";import {ownerListingView} from "@/application/listings";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){const account=await authenticatedAccount(request);if(!account)return Response.json({error:"Unauthorized"},{status:401});try{return Response.json(ownerListingView(await getContainer().listingService.publish(account,(await params).id)));}catch(error){return apiError(error);}}
