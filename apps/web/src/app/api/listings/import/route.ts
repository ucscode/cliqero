import {authenticatedAccount,apiError} from "../../http";import {getContainer} from "@/infrastructure/container";import type {TransferFormat} from "@/application/listing-transfer";
export async function POST(request:Request){const account=await authenticatedAccount(request);if(!account)return Response.json({error:"Unauthorized"},{status:401});try{const c=getContainer();await c.operators.requireCatalogueManager(account.id);const url=new URL(request.url),format=(url.searchParams.get("format")??contentFormat(request.headers.get("content-type"))) as TransferFormat,mode=url.searchParams.get("mode")??"create";if(!["json","csv","yaml"].includes(format)||!["create","upsert"].includes(mode))return Response.json({error:"Invalid import format or mode"},{status:400});const result=await c.listingTransfer.import(account,{format,mode:mode as "create"|"upsert",body:await request.text()});return Response.json(result,{status:207});}catch(error){return apiError(error);}}
function contentFormat(value:string|null){if(value?.includes("csv"))return "csv";if(value?.includes("yaml")||value?.includes("yml"))return "yaml";return "json";}
