import {getContainer} from "@/infrastructure/container";
export async function GET(_:Request,{params}:{params:Promise<{key:string[]}>}){const key=(await params).key.join("/"),container=getContainer();try{const record=await container.listingMediaRepository.findByStorageIdentity("filesystem","listing-media",key);if(!record||record.state!=="active")return new Response("Not found",{status:404});const provider=container.objectStorage.get("filesystem"),object=await provider.read!({provider:"filesystem",container:record.storageContainer,key});return new Response(Buffer.from(object.bytes),{headers:{"content-type":record.mimeType,"content-length":record.byteSize.toString(),"cache-control":"public, max-age=31536000, immutable","x-content-type-options":"nosniff"}});}catch{return new Response("Not found",{status:404});}}
